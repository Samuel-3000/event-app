import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Event and Crop App',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
